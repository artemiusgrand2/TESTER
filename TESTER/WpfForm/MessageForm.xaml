<Window x:Class="TESTER.MessageForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="MainWindow" Width="Auto" Height="Auto" WindowStyle="None" WindowStartupLocation="Manual"  ShowInTaskbar="False" AllowsTransparency="True" Background="Transparent" Topmost="True" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border  Style="{StaticResource MyBorderStyle}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition />
                <ColumnDefinition Width="5"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="2"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Name="textBlock_message" Text="" FontWeight="Bold" FontSize="15"  HorizontalAlignment="Center"  VerticalAlignment="Top" />
                <WrapPanel>
                    <Button  HorizontalAlignment="Center" VerticalAlignment="Center"  Height="25" Width="75"  Name="button_OK" Content="ДА" Click="button_OK_Click"></Button>
                </WrapPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
